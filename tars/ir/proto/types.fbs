namespace ace;

enum FrontendFramework : byte {
  NONE = 0,
  ONNX,
  CAFFE,
  TENSORFLOW,
  TFLITE,
  TORCH
}

enum DeviceType : byte {
  NONE = 0,
  X86,
  CUDA,
}

enum DataType : byte {
  NONE = 0,
  INT_8,
  INT_16,
  INT_32,
  INT_64,
  UINT_8,
  UINT_16,
  UINT_32,
  UINT_64,
  FLOAT_16,
  FLOAT_32,
  FLOAT_64,
}

enum DataFormat : byte {
    NONE = 0,
    NCHW,
    NHWC,
    NC4HW4,
    NHWC4,
}

enum OptimLevel : byte {
  NONE = 0,
  O0,
  O1,
  O2,
  O3,
}
